import "../../styles/admin.css";

export const metadata = {
  title: "Admin — Top Padel Alicante",
  robots: { index: false, follow: false },
};

export default function AdminRootLayout({ children }) {
  return children;
}
